{
  "type": "object",
  "title": "patch",
  "description": "Update an existing reply without retrieving any new context data. Or there is enough data in context to modify the reply.",
  "properties": {
    "origin_entity_id": {
      "type": "string",
      "description": "The ID of the origin message."
    }
  },
  "required": ["origin_entity_id"]
}
