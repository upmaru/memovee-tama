{
  "body": {
    "actions": [
      {
        "remove": {
          "index": "{{ data.index }}-*",
          "alias": "{{ data.index }}"
        }
      },
      {
        "add": {
          "index": "{{ data.index }}",
          "alias": "{{ data.index }}"
        }
      }
    ]
  }
}
